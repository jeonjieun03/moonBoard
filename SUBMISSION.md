# 제출 준비 메모 (초안 — 아직 제출 가능한 상태 아님)

## 아직 안 된 것 (제출 전 필수)

- [ ] 원본 zip의 fixture 5종+복구 연결 (`app/public/shared/synthetic.mjs` 완성)
- [ ] GitHub 저장소 생성 + push + Pages 배포
- [ ] 실제 KST 날짜 2건 확보 (서로 다른 날 — 기다리는 시간은 작업시간 제외)
- [ ] 아래 주소들을 실제 값으로 채우기

## 주소

- 결과물 주소(예정): `https://<github계정>.github.io/<저장소이름>/`
- 소스 주소: 배포 화면(Deployments)의 해당 배포를 열어 하단의 "View deployment"/커밋 링크 사용 — **커밋 해시(40자리)가 포함된 URL**이어야 함 (예: `https://github.com/<계정>/<저장소>/tree/<40자리 commit SHA>`). 저장소 기본 주소(`.../tree/main`)만 쓰면 안 됨.

## 짧은 확인 방법 4줄 (초안 — 실제 배포 후 문구 다시 확인)

1. 위치: 결과물 주소의 "일별 기록"과 "수신 테스트"로 갑니다.
2. 행동: 실패 종류 선택 → 재생 → "원자료 보기"를 누릅니다.
3. 통과: 실패 시 마지막 정상값·stale·해당 error_code·기존 행 수 유지, 복구 시 fresh/none·새 행 정확히 1건 추가. 실제 기록은 서로 다른 KST 날짜 2건이며 원자료·저장값·화면값과 전일 대비 변화 재계산이 일치합니다.
4. 안 될 때: 실패 사유·오래된 값 표시·다시 시도(재생) 버튼이 보입니다. 실제 기록이 1건뿐이면 두 번째 날짜 대기 중임을 표시합니다.

## AI와 나의 판단 3줄 (초안 — 본인 검토 후 확정 필요)

1. AI에게 맡긴 일: USNO API 연동 코드, 정규화·저장(dedup/전일대비) 로직, 화면 구현, GitHub Actions 수집·배포 워크플로 작성, self-test 작성.
2. 직접 판단한 일: 주제(달의 조명 비율)와 기준 위치(대구) 결정, 메인 값을 위상이 아닌 조명 비율(%)로 잡을지 검토·확정.
3. AI 제안을 따르지 않은 일: (해당 시 채워 넣기 — 예: ISS 위치는 동료와 겹쳐서 제외하고 달 주제로 결정)

## 제출 전 확인할 사항

- 결과물·소스 URL이 새 시크릿 창에서 계정 생성·로그인·인증·초대·비밀번호·OAuth·CAPTCHA 없이 열리는지 직접 확인 (T04-C01~C02, C29~C33).
- 실제 일별 기록이 서로 다른 KST 날짜 정확히 2건인지 배포된 `data/records.json`으로 확인.
- 플랫폼 발급 `t04_day` 영수증 2건의 발급 절차는 이 프로젝트 자료에 없으므로, 실제 제출 화면 안내를 별도로 확인.
